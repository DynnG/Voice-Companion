import { Conversation } from '../types/conversation';

/**
 * Initial conversations list.
 * Static/hardcoded demo tabs have been removed in favor of real user interview sessions.
 */
export const initialMockConversations: Conversation[] = [];
